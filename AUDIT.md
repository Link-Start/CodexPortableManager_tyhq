# Codex Portable Manager 功能保留审计

> 文档定位：当前 `1.1.0.0` 管理器的功能保留、安全边界与发布物审计。用户使用说明见 [README](README.md)，内部模块约束见 [架构说明](docs/ARCHITECTURE.md)。

## 审计对象

- 官方包：`OpenAI.Codex` MSIX
- 最近完成兼容实测的官方清单版本：`26.715.8383.0`（另以 `26.715.4045.0`、`26.715.2305.0`、`26.707.8168.0` 做过签名信任与兼容往返验证）
- 管理器版本：`1.1.0.0`
- 部署形态：普通目录中的 unpackaged Win32 应用

## 产品范围

- 管理器的核心职责是基于可信官方 MSIX 主动创建和维护普通目录便携版。
- 独立下载官方 MSIX 只提供可信安装包获取路径；使用 Windows 应用安装程序完成安装后，该版本属于官方桌面版及 Windows 包生命周期。
- 可选兼容层不支持官方桌面版，不修改 `C:\Program Files\WindowsApps`，也不通过接管 ACL、重签包或其他方式绕过 Windows 的包保护和更新机制。

## 已保留

- 官方 MSIX 主体内容，不主动裁剪应用文件。
- `app/ChatGPT.exe`、`resources/app.asar`、`resources/codex.exe`、沙箱组件、插件和语言资源。
- `%APPDATA%`、`%LOCALAPPDATA%\OpenAI` 与 `%USERPROFILE%\.codex` 用户数据路径。
- `codex://` URL 协议。
- `.csv`、`.tsv`、`.xls`、`.xlsm`、`.xlsx` 和 `.skill` 文件打开方式。
- `com.openai.codex` AppUserModelID、开始菜单和桌面快捷方式。
- 官方托盘图标及可选的独立 `Codex.ico`、窗口图标和 EXE 图标兼容处理。
- 当前用户级 `App Paths`、文件关联和通知标识。

## 由管理器替代

- Microsoft Store 自动更新由 Display Catalog 选包、Windows Update 官方分发、可信下载、缓存和本地更新事务替代。
- Store 回滚由本地 `.previous` 槽位、受信任的缓存低版本部署及可恢复事务替代。
- Store 卸载由便携版所有权验证和安全递归删除替代。
- Store 缓存维护由最近版本、无效缓存和日志保留策略替代。

## 无法等价保留的 MSIX 属性

- Windows 包管理数据库中的 Package Family Name、PRAID 和真实包身份。
- WindowsApps ACL 和 Store 包信任边界。
- Microsoft Store 安装记录、许可证、自动更新、修复和卸载生命周期。
- 包化 AUMID、VisualElements 和依赖包身份的未来功能。
- 可选兼容修改完成后与原始 BlockMap、Code Integrity catalog 和包签名负载的字节级一致性。

当前官方清单关闭了文件系统和注册表虚拟化，没有声明外部 framework 依赖，主程序为 `runFullTrust` 桌面应用。因此普通目录部署能够保留当前主要运行功能，但不能被视为 Store 包的等价克隆。

## 官方程序包信任

- 下载地址保留 Windows Update 返回的 HTTP 或 HTTPS 原始协议，不擅自升级或降级；仅接受微软分发域名，随后必须通过文件摘要、MSIX 签名和包身份校验。
- 断网恢复只接受与本地断点精确衔接的 `206 Content-Range`，并核对总字节数；CDN 忽略 Range 时从头覆盖临时文件，不允许把完整响应追加到旧片段。暂停和重试不改变最终 SHA-256、签名与包身份校验边界。
- Windows NCSI 离线状态只降低 CDN 探测频率，不能无限阻止真实请求；达到当前指数退避截止点后必须主动探测，系统恢复事件或轮询转为在线时立即探测。HTTP `408`、`429`、`500`、`502`、`503`、`504` 和瞬时传输异常进入有限重试。
- 增量 Range 与完整下载都按百分比变化或约 250 ms 向 UI 更新字节、速度和预计剩余时间；高频进度不逐条写入运行日志。
- 核对 Display Catalog 的产品 ID、Package Family Name、包名、四段版本、架构、资源 ID 和 Publisher ID。
- 将 Display Catalog 与 Windows Update 返回的文件字节数和 SHA-256 Base64 摘要交叉校验，并继续执行签名、Publisher、包身份和架构校验。
- 使用 Windows 信任 API 校验 MSIX 数字签名。
- 核对签名证书发布者、Manifest Publisher、包名、版本和 `ProcessorArchitecture`。
- 校验 `AppxSignature.p7x` 与 `AppxManifest.xml` 的唯一性和基本结构。
- 从可信校验到解包结束保持 MSIX 只读文件锁，从同一句柄复核 SHA-256，并将后续路径型签名、证书读取、复制和解包绑定到该句柄解析出的稳定物理路径。
- 解包后根据 `AppxBlockMap.xml` 逐文件、逐块校验 SHA-256、原始字节数和路径边界。

## 部署和文件系统可靠性

- 操作锁使用 `Global\` 命名互斥体并授予跨账户同步权限；锁键由已存在路径句柄的卷序列号、128 位文件 ID 与剩余相对路径构成，句柄身份不可用时回退到最终 NT 路径，可合并 junction、映射盘、UNC、SUBST 和 8.3 路径别名。
- 缓存校验、下载、发布、签名验证和解包使用按目标缓存路径命名的独立长时锁，异步等待支持取消。
- 更新使用 `transaction-old` 轮换 current、previous 和旧回滚备份；新 current 与 `.previous` 提交后立即向界面返回，独立后台进程再凭 Armed receipt 回收 `transaction-old` 并完成 journal。窗口退出不会中断，进程或系统中断则由后续启动继续恢复。`.previous` 交换使用独立回滚事务，缓存低版本回滚复用同一 staging 与更新型 journal，并把操作前 current 保留为新的 `.previous`。
- 启动新操作时检查并恢复可确认的中断事务；拓扑不明确时拒绝猜测和破坏性处理。
- 安装及回滚槽位使用同一安装 ID 的所有权 marker；非 Codex payload 或不匹配目录拒绝破坏性操作。
- 受管安装根及其现有祖先必须是普通目录；junction、符号链接和其他重解析点在读取 payload 或 marker 前即被拒绝。
- 删除统一使用句柄式原语：空目录在同一句柄上确认无子项后直接删除，禁止从“空目录”路径进入递归；普通文件相对稳定父句柄 no-follow 打开；受管目录树递归删除必须复验持久 File ID。重解析点只删除链接对象。
- 磁盘根目录禁止作为安装根，删除范围仍受允许父目录约束。
- 旧缓存迁移逐文件复制、刷新并校验大小和 SHA-256；删除源文件时复验复制前绑定的 File ID，同名冲突或身份变化时保留双方，仅删除已确认迁移成功的源文件和空目录。
- 兼容设置在停止目录内进程前先复验安装健康、所有权、安装 ID 和安装根 File ID，停止后再次确认目标未变化；非 Codex、篡改或归属不明的目录不会先被结束进程。
- 兼容事务逐级拒绝受保护制品路径中的重解析点祖先；journal 绑定安装根 File ID、原安装 ID 和 marker 要求，兼容备份目录身份在复制前持久化。marker 缺失或损坏只允许在 `FilesChanged` 阶段降级恢复，且其他受保护制品必须处于 journal 已知原始态或目标态；陌生摘要、较早阶段、已提交阶段、同路径替换安装或同名替换备份均失败关闭。身份缺失、备份清理失败时保留 journal、备份和现场。
- 缓存原子发布及 Windows MSIX 签名/身份验证期间临时关闭取消，阶段结束后恢复；永久提交阶段不会被后续进度误重新开启取消。用户指定路径的官方 MSIX 保存改为分块可取消复制，完整刷新后才原子替换原目标。

## 存储保留与崩溃清理

- 正式 MSIX 默认保留最新两个。
- `.invalid-<GUID>` 最多保留一个，超过 7 天可清理。
- 超过 24 小时且严格匹配的 `.download-<GUID>.msix` 和 `.materialize-<GUID>.msix` 临时包可清理；名称不匹配、仍被缓存锁使用或身份无法确认的文件保留。
- 会话、存储错误、致命错误和部署后后台清理日志默认保留 30 天，总量上限 50 MiB。
- 存储维护只清理严格匹配管理器命名规则的顶层文件，未知文件保持不变。
- `.cpm-<GUID>` 工作目录写入包含 WorkId、安装根哈希、目录 File ID 和创建时间的 marker。
- 仅清理属于同一安装、marker 有效且超过 24 小时的顶层工作目录；未知、新建、重解析点或无法安全删除的目录会保留剩余内容并记录提示。

## 可选兼容层

- 以下兼容处理只作用于本工具拥有并验证通过的普通目录便携版，不以官方桌面版的 WindowsApps 目录作为输入目标。
- 图标兼容只处理已验证 staging 副本；资源缺失、格式变化或写入失败时保留官方文件并继续部署。
- 模型 catalog 补丁先按可解码的 `availableModels/has/model/hidden` 属性名和受管标记建立轻量候选索引，再使用内嵌 Esprima 定位唯一的模型可见性过滤，并验证 `.has(model)` 集合沿祖先作用域绑定到 `availableModels` 解构参数，同时要求 webview 中存在官方 `available_models` 配置来源。索引异常或无法得到候选时回退全量语义扫描，因此该统一规则覆盖独立 chunk、合并大 chunk、属性转义和后续文件改名，不依赖版本号、文件名、变量名或配置键出现次数。候选不唯一、绑定被遮蔽或来源不足时保持官方关闭，界面禁用该项并显示原因。
- 中文菜单资源按实际消费者增减翻译键，动态推理强度按键族处理；官方新增、删除或改名少量键时只应用能够确认的部分，未匹配部分保持官方状态并给出提示。
- 原生菜单脚本将顶部菜单、托盘静态标签、托盘退出文本、性能跟踪文本和共用语言设置联动层作为独立能力定位、标记和恢复。单个官方组件消失时不否定其余可验证组件；所有当前可支持组件均已启用时，状态可记为 `Patched` 并由更新安全继承，同时保留跳过说明。
- 菜单、托盘和设置联动入口按 AST 成员关系、数据流和唯一消费者定位；函数表达式、变量改名、命名空间层级、菜单条目增减或语言 chunk 改名不会单独使整个功能失配。完全没有可识别能力且没有受管标记时保持 `UnmanagedOrOfficial`；候选不唯一、未知或残缺受管标记、恢复区间无法证明时仍对风险部分失败关闭。
- Windows 沙箱兼容只修改 `app.asar` 中唯一 Electron 主进程入口的账户环境，官方签名 helper 始终保持原位且不进入兼容写入事务。只有裸用户名 SID 解析错误、`USERDOMAIN\USERNAME` 能可靠绑定当前 token SID 时才注入；未知脚本结构或账户关系无法确认时保持失败关闭。兼容配置失败时保留当前 `app.asar` 并降级为警告。
- 纯启动、`.previous` 交换和启动入口修复不会重新应用四项功能调整；新安装默认全部关闭。更新、修复和缓存包回滚只在尚未激活的官方 staging 中继承旧安装文件的可确认实际状态，不读取持久开关；目标版本无法安全支持的继承项不写入文件，按官方关闭状态完成并在安装后禁用开关。明确缓存回滚遇到不可确认的开发期兼容状态时，同样以全部关闭部署目标官方文件并保留原 current 为 `.previous`，不迁移旧配方。只有独立应用操作会按当前会话草稿修改现有便携版。
- Shell 集成按管理器状态同步；单项写入或清理失败采用 best-effort 处理并记录警告。

## 权限和威胁模型边界

管理器不会强制修改用户选择安装目录的 DACL，以保留普通目录的可写、迁移、备份和手工修改自由。该策略意味着：

- 有权写入安装目录的同一用户、管理员或其他授权主体仍可修改普通应用文件；
- 普通目录不具备 WindowsApps ACL 和 MSIX 包身份带来的隔离；
- 沙箱提权 helper 和官方 MSIX 具有额外校验，但这不等于为整个安装目录建立不可篡改边界。

如需更严格的本机多用户隔离，应由用户选择受保护目录并自行配置 DACL。未来官方新增依赖包身份的功能时，需要重新审计和适配。

## 管理器发布物真实性

- 当前构建产物为单 EXE，内嵌第三方程序集按固定 SHA-256 校验，Release、开发副本和测试绑定副本可以通过摘要确认来自同一次确定性构建。
- 当前仓库构建流程尚未执行 Authenticode 代码签名；这不影响管理器对官方 MSIX 的签名验证，但意味着 Windows 无法用发布者证书确认管理器 EXE 本身的来源。
- `v1.1.0` Release 发布物包含 `CodexPortableManager.exe` 和 `SHA256SUMS.txt`。正式 EXE 为 1,820,672 字节，SHA-256 为 `372E0CD62ED063622AB15083E0E912B037D125CE28008213461F01FAC19ED156`；本地 Release 构建、190 项隔离回归、离线 Store/网络、路径自动刷新、PowerShell 语法和真实官方 MSIX 信任验证均已通过，GitHub Windows 验证由发布标签推送触发并作为正式 Release 发布门禁。
- 后续公开发布仍应优先增加带可信时间戳的 Authenticode 签名，并继续同步发布最终 EXE 的 SHA-256。签名和校验和生成必须位于最终发布文件确定之后，不能复用官方 MSIX 的信任结果替代管理器自身的发布认证。
